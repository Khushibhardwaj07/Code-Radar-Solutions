#include <stdio.h>
int main(){
    char alpha;
    scanf("%c",&alpha);
    if (A<=alpha && alpha<=Z){
        printf("Uppercase");
    }
    else{
        printf("Lowercase");
    }
    return 0;
}